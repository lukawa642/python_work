2.1传值类型
*不可变类型

*可变类型
2.2函数参数类型
*必备参数（位置参数）
1）必备，不传值会报错
2）传的值有顺序，从左到右，每个参数一一对应
*关键字参数
1）赋值时指定参数名，不按位置顺序
2）如果和必备参数混用，必须放在位置参数后边
*默认函数
对于一些调用时非必选的参数，可设置成默认参数，这样即使用户不填，也不影响函数正常的运行。
*不定长参数
   *args **kwargs
注意不定长参数的妙用
